import Calculator from './Calculator';
import './Math.css';

const Math = () => (
  <div className="math">
    <h1>Let&apos;s do some math!</h1>
    <Calculator />
  </div>
);

export default (Math);

const Quote = () => (
  <div>
    <h1>
      Mathematics is not about numbers, equations, computations,
      or algorithms: it is about understanding. -William Pault Thurston
    </h1>
  </div>
);

export default (Quote);
